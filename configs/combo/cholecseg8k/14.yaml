# @package _global_

defaults:
- /data/cholecseg8k@data
model:
  fintuned_model_path: null
  trainable_modules:
  - mask_decoder
  prompt_type: mask
combo:
  name: cholecseg8k_mask_md
data_module:
  data: ${data}

