# @package _global_

defaults:
- /data/endovis17@data
model:
  fintuned_model_path: null
  trainable_modules:
  - memory_encoder
  - memory_attention
  prompt_type: box
combo:
  name: endovis17_box_mem
data_module:
  data: ${data}

