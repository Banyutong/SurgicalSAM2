# @package _global_

defaults:
- /data/endovis17@data
model:
  fintuned_model_path: null
  trainable_modules:
  - mask_decoder
  - prompt_encoder
  - image_encoder
  prompt_type: mask
combo:
  name: endovis17_mask_md+pe+ie
data_module:
  data: ${data}

