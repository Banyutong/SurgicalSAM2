# @package _global_

defaults:
- /data/cholecseg8k@data
model:
  fintuned_model_path: null
  trainable_modules:
  - mask_decoder
  - prompt_encoder
  - image_encoder
  prompt_type: mask
combo:
  name: cholecseg8k_mask_md+pe+ie
data_module:
  data: ${data}

