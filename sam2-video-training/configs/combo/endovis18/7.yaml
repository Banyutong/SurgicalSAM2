# @package _global_

defaults:
- /data/endovis18@data
model:
  fintuned_model_path: null
  trainable_modules:
  - memory_encoder
  - memory_attention
  prompt_type: mask
combo:
  name: endovis18_mask_mem
data_module:
  data: ${data}

