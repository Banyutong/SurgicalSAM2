# @package _global_

defaults:
- /data/endovis18@data
model:
  fintuned_model_path: null
  trainable_modules:
  - memory_encoder
  - memory_attention
  prompt_type: point
combo:
  name: endovis18_point_mem
data_module:
  data: ${data}

