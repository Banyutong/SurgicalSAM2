# @package _global_

defaults:
- /data/endovis18@data
model:
  fintuned_model_path: null
  trainable_modules:
  - mask_decoder
  - prompt_encoder
  - image_encoder
  prompt_type: point
combo:
  name: endovis18_point_md+pe+ie
data_module:
  data: ${data}

